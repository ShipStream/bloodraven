---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: mysqlfailovergroups.shipstream.io
spec:
  group: shipstream.io
  names:
    kind: MysqlFailoverGroup
    listKind: MysqlFailoverGroupList
    plural: mysqlfailovergroups
    singular: mysqlfailovergroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.activeSite
      name: Active
      type: string
    - jsonPath: .status.sites[0].state
      name: Site-A
      type: string
    - jsonPath: .status.sites[1].state
      name: Site-B
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MysqlFailoverGroup is the Schema for the mysqlfailovergroups
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MysqlFailoverGroupSpec defines the desired state of MysqlFailoverGroup.
            properties:
              az:
                description: AZ is the availability zone identifier.
                minLength: 1
                type: string
              cloneTimeout:
                default: 3600
                description: |-
                  CloneTimeout is the timeout in seconds for CLONE INSTANCE operations. It
                  controls the session variables net_read_timeout and net_write_timeout
                  (set per-session before cloning) and the global variable clone_ddl_timeout
                  (GLOBAL-only; set via SET GLOBAL before cloning and also written to
                  my.cnf). Default: 3600 (1 hour).
                minimum: 60
                type: integer
              cloudflare:
                description: Cloudflare contains the Cloudflare DNS configuration
                  for failover.
                properties:
                  apiTokenSecretRef:
                    description: APITokenSecretRef references the secret containing
                      the Cloudflare API token.
                    properties:
                      key:
                        description: Key within the Secret.
                        minLength: 1
                        type: string
                      name:
                        description: Name of the Secret.
                        minLength: 1
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  zoneID:
                    description: ZoneID is the Cloudflare zone ID.
                    minLength: 1
                    type: string
                required:
                - apiTokenSecretRef
                - zoneID
                type: object
              failoverCooldown:
                description: 'FailoverCooldown is the minimum time between failovers.
                  Default: 60m'
                type: string
              failureThreshold:
                default: 3
                description: 'FailureThreshold is how many consecutive failures before
                  marking unreachable. Default: 3'
                format: int32
                minimum: 1
                type: integer
              image:
                default: mysql:9.6
                description: 'Image is the MySQL container image. Default: mysql:9.6'
                type: string
              mysqlConf:
                additionalProperties:
                  type: string
                description: MysqlConf allows overriding default my.cnf settings.
                type: object
              pollInterval:
                description: 'PollInterval is how often to poll MySQL instances. Default:
                  2s'
                type: string
              recoveryThreshold:
                default: 2
                description: 'RecoveryThreshold is how many consecutive successes
                  before marking recovered. Default: 2'
                format: int32
                minimum: 1
                type: integer
              replication:
                description: Replication configures replication health monitoring.
                properties:
                  maxLagSeconds:
                    default: 300
                    description: 'MaxLagSeconds is the maximum acceptable replication
                      lag before marking degraded. Default: 300'
                    format: int64
                    minimum: 0
                    type: integer
                type: object
              secretName:
                description: SecretName references the secret containing MySQL credentials.
                minLength: 1
                type: string
              sidecar:
                description: Sidecar configures sidecar behavior.
                properties:
                  bloodravenAddress:
                    description: |-
                      BloodravenAddress is the address of the Bloodraven operator health endpoint.
                      Default: bloodraven.<namespace>.svc.cluster.local:8082
                    type: string
                  leaseTimeout:
                    description: 'LeaseTimeout is how long before a sidecar lease
                      expires. Default: 20s'
                    type: string
                  peerCheckInterval:
                    description: 'PeerCheckInterval is how often the sidecar checks
                      its peer. Default: 5s'
                    type: string
                type: object
              sidecarImage:
                description: SidecarImage is the image used for the sidecar/init container.
                type: string
              sites:
                description: |-
                  Sites defines the two sites that form this failover group.
                  Exactly two sites must be specified; either can be active.
                items:
                  description: SiteSpec defines the configuration for a single site
                    in the failover group.
                  properties:
                    lbIP:
                      description: LBIP is the load balancer IP for DNS failover.
                      minLength: 1
                      type: string
                    name:
                      description: Name is the site identifier (e.g. "iad", "pdx").
                      minLength: 1
                      type: string
                    resources:
                      description: Resources defines the compute resources for the
                        MySQL container.
                      properties:
                        claims:
                          description: |-
                            Claims lists the names of resources, defined in spec.resourceClaims,
                            that are used by this container.

                            This field depends on the
                            DynamicResourceAllocation feature gate.

                            This field is immutable. It can only be set for containers.
                          items:
                            description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                            properties:
                              name:
                                description: |-
                                  Name must match the name of one entry in pod.spec.resourceClaims of
                                  the Pod where this field is used. It makes that resource available
                                  inside a container.
                                type: string
                              request:
                                description: |-
                                  Request is the name chosen for a request in the referenced claim.
                                  If empty, everything from the claim is made available, otherwise
                                  only the result of this request.
                                type: string
                            required:
                            - name
                            type: object
                          type: array
                          x-kubernetes-list-map-keys:
                          - name
                          x-kubernetes-list-type: map
                        limits:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: |-
                            Limits describes the maximum amount of compute resources allowed.
                            More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                          type: object
                        requests:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: |-
                            Requests describes the minimum amount of compute resources required.
                            If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                            otherwise to an implementation-defined value. Requests cannot exceed Limits.
                            More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                          type: object
                      type: object
                    storage:
                      description: Storage configures the persistent volume for this
                        site.
                      properties:
                        size:
                          anyOf:
                          - type: integer
                          - type: string
                          description: Size is the requested storage size.
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        storageClassName:
                          description: StorageClassName is the name of the StorageClass.
                          minLength: 1
                          type: string
                      required:
                      - size
                      - storageClassName
                      type: object
                    zone:
                      description: Zone is the Kubernetes topology zone for node selection.
                      minLength: 1
                      type: string
                  required:
                  - lbIP
                  - name
                  - storage
                  - zone
                  type: object
                maxItems: 2
                minItems: 2
                type: array
              terminationGracePeriodSeconds:
                default: 30
                description: 'TerminationGracePeriodSeconds is the grace period for
                  MySQL container shutdown. Default: 30'
                format: int64
                minimum: 1
                type: integer
              tls:
                description: TLS configures TLS for the MySQL instances.
                properties:
                  issuerRef:
                    description: IssuerRef references a cert-manager Issuer or ClusterIssuer.
                    properties:
                      kind:
                        description: Kind of the issuer (Issuer or ClusterIssuer).
                        enum:
                        - Issuer
                        - ClusterIssuer
                        type: string
                      name:
                        description: Name of the issuer.
                        minLength: 1
                        type: string
                    required:
                    - kind
                    - name
                    type: object
                  secretName:
                    description: |-
                      SecretName is the name of the Secret containing the TLS certificates.
                      cert-manager should be configured to create this secret from the IssuerRef.
                    minLength: 1
                    type: string
                required:
                - issuerRef
                - secretName
                type: object
              updateStrategy:
                default: OrderedUpdate
                description: 'UpdateStrategy controls how spec changes are rolled
                  out. Default: OrderedUpdate'
                enum:
                - OrderedUpdate
                - Recreate
                type: string
            required:
            - az
            - cloudflare
            - secretName
            - sites
            type: object
          status:
            description: MysqlFailoverGroupStatus defines the observed state of MysqlFailoverGroup.
            properties:
              activeSite:
                description: ActiveSite is the name of the site currently acting as
                  primary (writable).
                type: string
              conditions:
                description: Conditions represent the latest available observations.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastFailover:
                description: LastFailover is the timestamp of the last failover event.
                format: date-time
                type: string
              lastFailoverTarget:
                description: LastFailoverTarget is the site that was last promoted
                  during failover.
                type: string
              sites:
                description: Sites is the observed state of each site, parallel to
                  spec.sites.
                items:
                  description: SiteStatus describes the observed state of a single
                    site.
                  properties:
                    gtidExecuted:
                      description: GtidExecuted is the GTID executed set (populated
                        when replication status is enriched).
                      type: string
                    lastSeen:
                      description: LastSeen is the last time this site was successfully
                        polled.
                      format: date-time
                      type: string
                    name:
                      description: Name is the site identifier, mirrored from spec
                        for convenience.
                      type: string
                    replicating:
                      description: Replicating indicates whether replication is running
                        (populated when replication status is enriched).
                      type: boolean
                    secondsBehindSource:
                      description: SecondsBehindSource is the replication lag in seconds
                        (populated when replication status is enriched).
                      format: int64
                      type: integer
                    state:
                      description: 'State is the current state: writable, read-only,
                        unreachable, or unknown.'
                      enum:
                      - writable
                      - read-only
                      - unreachable
                      - unknown
                      type: string
                  type: object
                type: array
              updatePhase:
                description: UpdatePhase indicates the current phase of an ordered
                  update, empty if not updating.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
