# E2E trigger workflow — nightly, manual, and PR-label-gated.
# The reusable workflow is in .github/workflows/_e2e.yml.
name: E2E

on:
  # Nightly release-profile run
  schedule:
    - cron: "0 5 * * *" # 05:00 UTC daily

  # Manual dispatch with profile selection
  workflow_dispatch:
    inputs:
      profile:
        description: "Chaos profile (smoke|release|full)"
        required: false
        default: "release"
        type: choice
        options:
          - smoke
          - release
          - full

  # PR label gate: run smoke while the "e2e" label is present.
  pull_request:
    types: [opened, reopened, synchronize, labeled]

permissions:
  contents: read

jobs:
  # Skip PR-triggered runs unless the "e2e" label is present.
  e2e:
    if: >-
      github.event_name == 'schedule' ||
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'e2e'))
    uses: ./.github/workflows/_e2e.yml
    with:
      profile: ${{ github.event_name == 'pull_request' && 'smoke' || (github.event.inputs.profile || 'release') }}

  # Label gating is intentionally PR-only: schedule and manual runs always
  # exercise encryption, while release publication uses release.yml's gate.
  encryption:
    if: >-
      github.event_name == 'schedule' ||
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'e2e'))
    uses: ./.github/workflows/_e2e.yml
    with:
      # Ordered least- to most-destructive: 48 and 50 leave the cluster
      # healthy, 51 rebuilds the replica datadir. Together they are the
      # minimum set from .tmp/chaos-plan-v1.0.md that makes "encryption
      # validated in CI" true rather than rotation-only.
      scenario: 48-keyring-seal-and-rotation,50-encrypted-pod-replace,51-encrypted-reclone
      enable-encryption: true
      # Three scenarios on one cluster; 51 alone budgets 20m for the clone.
      timeout-minutes: 150