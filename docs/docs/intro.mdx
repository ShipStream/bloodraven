---
sidebar_position: 1
slug: /
---

# Bloodraven

Bloodraven is a Kubernetes operator for managing MySQL async replication failover groups across two sites. It automates failover detection, promotion, DNS steering, and application workload migration so that a site-level MySQL outage is resolved without human intervention.

## Who this is for

| Reader | Use these docs to |
|---|---|
| New user | Try Bloodraven locally and create a first failover group. |
| Platform operator | Install the operator, define production guardrails, and run go-live checks. |
| Application developer | Connect safely and handle failover reconnect behavior. |
| On-call responder | Map alerts to runbooks and verify recovery under pressure. |
| Backup owner | Configure, verify, and restore backups without reading the full CRD reference first. |

## What Bloodraven does not do

- It does not provide synchronous replication or zero RPO after sudden primary loss.
- It does not replace external-dns, cert-manager, Prometheus, Grafana, or your object store.
- It does not make application connection pools failover-aware automatically.
- It does not reconcile divergent writes for you after split-brain.
- It does not make PVC-local backups durable after cluster or storage loss.

## User journeys

| Journey | Path |
|---|---|
| New user | [Getting Started](./getting-started.mdx) → [Playground](./playground.mdx) → [App Integration](./app-integration.mdx) |
| Platform operator | [Production Install](./install-production.mdx) → [Production Hardening](./production-hardening.mdx) → [Monitoring](./monitoring.mdx) |
| On-call | [Operations Overview](./operations-overview.mdx) → [Failure Mode Matrix](./failure-mode-matrix.mdx) → [Runbooks](./runbooks.mdx) |
| Backup owner | [Backup Overview](./backup-overview.mdx) → [S3](./backup-s3.mdx) or [PVC](./backup-pvc.mdx) → [Verification](./backup-verification.mdx) → [Restore](./backup-restore.mdx) |

## What it does

- **Monitors** two MySQL instances (one per site) via a 2-second polling loop with configurable failure and recovery thresholds
- **Detects** when the active primary becomes unreachable and **promotes** the standby replica to primary
- **Steers traffic** by updating a DNSEndpoint CR (consumed by external-dns) to point to the new primary's load balancer IP
- **Migrates workloads** by tainting nodes at the failed site so Kubernetes evicts application pods
- **Prevents split-brain** with sidecar self-fencing: if both the operator and peer are unreachable, the sidecar sets `super_read_only=ON` as a safety net (see [Operator availability](./operator-availability.mdx) and [Durability and RPO](./durability-and-rpo.mdx) for exact behaviour and bounds)
- **Broadcasts status** over a WebSocket API for dashboards and alerting integrations
- **Backs up and restores** via mysqlsh `util.dumpInstance()` / `util.loadDump()` to S3 or PVC, with scheduled CronJobs, structured retention, retries, and automatic artifact cleanup

:::tip
**Using an AI agent?** This documentation is available as `llms.txt` and `llms-full.txt` at the site root for consumption by LLM-based tools. Give your agent context with a prompt like:

```
Read https://bloodraven.readthedocs.io/en/latest/llms-full.txt and help me configure a MysqlFailoverGroup for two sites with async replication and automatic DNS failover.
```

:::

:::info When to use Bloodraven
Bloodraven targets the **two-site, accept-non-zero-RPO** deployment.
If you need synchronous writes and zero RPO on primary loss, read
[Why not Group Replication?](./why-not-group-replication.mdx) first
to make sure the tradeoffs match your use case.
:::

## Components

| Component | Description |
|---|---|
| `bloodraven` | The operator binary. Runs as a Deployment, watches `MysqlFailoverGroup` CRs, reconciles MySQL state. |
| `bloodraven-sidecar` | Runs alongside each MySQL container. Provides health probes and self-fencing when the operator is unreachable. |

## Custom Resource

Bloodraven introduces a single CRD:

- **`MysqlFailoverGroup`** (`shipstream.io/v1alpha1`) -- Declares a pair of MySQL instances across two named sites, their storage, networking, DNS, TLS, and failover tuning.

```yaml
apiVersion: shipstream.io/v1alpha1
kind: MysqlFailoverGroup
metadata:
  name: orders
spec:
  credentials:
    operatorSecret: mysql-operator-creds
    appSecret: mysql-app-creds
  dns:
    hostname: orders.az.example.com
    ttl: 60
  sites:
    - name: iad
      zone: us-east-1a
      taintNodeSelector:
        shipstream.io/failover-group.orders: "true"
        shipstream.io/site.orders: iad
      lbIP: 10.0.1.1
      storage:
        size: 50Gi
        storageClassName: gp3
    - name: pdx
      zone: us-west-2a
      taintNodeSelector:
        shipstream.io/failover-group.orders: "true"
        shipstream.io/site.orders: pdx
      lbIP: 10.0.2.1
      storage:
        size: 50Gi
        storageClassName: gp3
```

## Next steps

- [Architecture](./architecture.mdx) -- How the operator, sidecars, and MySQL instances interact.
- [Getting Started](./getting-started.mdx) -- Install the operator and create your first failover group.
- [Production Install](./install-production.mdx) -- Production dependencies, Helm values, CRD ownership, and verification.
- [Playground](./playground.mdx) -- Try Bloodraven locally on k3d, kind, or minikube with a live dashboard and chaos tools.
- [Backup Overview](./backup-overview.mdx) -- Choose S3 or PVC, configure schedules, and verify recoverability.
- [Runbooks](./runbooks.mdx) -- Incident response entry points.
- [CRD Reference](./crd-reference.mdx) -- Complete spec and status field reference.
